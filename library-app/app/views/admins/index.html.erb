<% @admins.each do |admin| %>
    <tr>
      <td><%= admin.first_name %></td>
      <td><%= admin.last_name %></td>
      <td><%= admin.email %></td>
      <td><%= link_to 'Delete Admin', admin, method: :delete, data: { confirm: "Are you sure you want to delete the admin?" } %></td>
      <br>
    </tr>
<% end %>
<td><%= link_to 'Back', current_admin %></td>